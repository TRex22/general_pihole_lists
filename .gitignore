# Malicious domain scraper cache - contains scraped article metadata, not committed
.DS_Store
scripts/malicious_domains_cache.json
scripts/malicious_domains_cache.json.*
scripts/thn_scrape_cache.json
scripts/blocklist_project_cache.json

# Malicious package scraper cache
scripts/malicious_packages_cache.json
scripts/malicious_packages_cache.json.*

# AI blocklist reviewer cache — tracks (model, domain) pairs already reviewed
scripts/ai_review_cache.json

# Temporary file used as dummy output_file placeholder during package scraping
databases/.pkg_tmp

# Compiled macOS OCR helper binary (built from ocr_macos.swift on first run)
scripts/ocr_macos

log/

# Pi-hole query log extracts — generated by scripts/extract_pihole_logs.rb
query_logs/

# Bundler — gems installed by CI or local bundle install --path
vendor/bundle
.bundle/
.bundle/config

tmp/

